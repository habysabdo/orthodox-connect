import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.orthodoxconnect.app',
  appName: 'Orthodox Connect',
  webDir: 'dist'
};

export default config;
